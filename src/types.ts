export interface LastPassData {
  url: string;
  username: string;
  password: string;
  extra: string;
  name: string;
  grouping: string;
  fav: string;
}

export interface BitWardenData {
  folder: string;
  favorite: string;
  type: string;
  name: string;
  notes: string;
  fields: string;
  login_uri: string;
  login_username: string;
  login_password: string;
  login_totp: string;
}
